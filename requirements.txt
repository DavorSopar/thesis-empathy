# Core scientific stack for the EDA and baseline notebooks.
# Versions are those the notebooks were last run and verified against;
# loosen the pins if you need to.
numpy>=1.26,<3
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13

# Deep learning stack for RoBERTa fine-tuning
torch>=2.0
transformers>=4.35
datasets>=2.14

# Notebook execution
jupyterlab>=4.0
ipykernel>=6.29
